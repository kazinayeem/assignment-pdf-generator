import { InterviewHubClient } from "@/components/career/interview-hub-client";

export const metadata = { title: "Interview Prep Hub | Career Hub" };

export default function InterviewPage() {
  return <InterviewHubClient />;
}
