import { CoverLetterClient } from "@/components/career/cover-letter-client";

export const metadata = { title: "Cover Letter Builder | Career Hub" };

export default function CoverLetterPage() {
  return <CoverLetterClient />;
}
