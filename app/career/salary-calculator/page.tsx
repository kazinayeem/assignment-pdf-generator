import { SalaryCalculatorClient } from "@/components/career/salary-calculator-client";

export const metadata = { title: "Salary Calculator | Career Hub" };

export default function SalaryCalculatorPage() {
  return <SalaryCalculatorClient />;
}
